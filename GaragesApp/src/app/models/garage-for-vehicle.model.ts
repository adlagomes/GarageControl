// src/app/models/garage-for-vehicle.model.ts
// Esta interface representa o DTO simplificado de Garagem
// que é incluído dentro do VehicleDto do backend.
export interface GarageForVehicle {
  id: number;
  name: string;
}